#include <iostream>
#include <string>
using namespace std;
int main()
{

string text;
cout << " Skriv in din text: " ;
cin >> text;

if ( (older < 30) && (kon == "man") || ( barn == "Nej") && (kon == "man"))
{  
  cout << "Ratt svar, Du klarade det po ett f?rs?k" << endl; 
}

else {
	cout << "Fel svar, Du klarade inte det " << endl;
}
  return 0;
} 
